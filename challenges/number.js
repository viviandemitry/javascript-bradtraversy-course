const x = Math.random(); // 0,483948298423942394

const x = Math.floor(Math.radom() * 100 + 1);// Here you get one number between 1 and 100.

//Get the sum
const sum = x + y;
const sumOutput = ´${x} + ${y} = ${sum}´; // x + y = sum (with de numbers)

//Get the difference
const diff = x - y;
const diffOutput = ´${x} - ${y} = ${diff}´;

//Get the product
const prod = x * y;
const prodOutput = ´${x} * ${y} = ${prod}´;

//Get the quotient
const quot = x / y;
const quotOutput = ´${x} / ${y} = ${quot}´;

//Get the remainder
const rm = x % y;
const rmOutput = ´${x} % ${y} = ${rm}´;
